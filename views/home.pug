extends layout

include mixins/_pollCard

block content
  .homepage
    h1 Make your opinion count!
    if user
      p Welcome, #{user.name}!
    p Check all the available polls below and choose right now:
    if !user
      .pro-box
        h3 PRO MODE:
        h2 Create a free account
        p ✍️ and start your own polls
        p 📖 keep track of the results
        p 💣 delete them
        p 🚗 add new options to other user's polls!
        a.button(href="/register") 🎢 Free sign in Now!
    p Choose a poll here, and vote to see the result chart...
    .polls
      each poll in polls
        +pollCard(poll)